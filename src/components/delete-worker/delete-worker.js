import React, {Component} from 'react';

export default class DeleteWorker extends Component {
    render() {
        return(
            <button className="btn btn-danger">Удалить сотрудника</button>
        )
    }
}